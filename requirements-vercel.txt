# Minimal Vercel runtime dependencies. Heavy ML/training deps removed.
# Keep only runtime essentials to keep the deployment bundle small.
Django==6.0.4
whitenoise[brotli]==6.11.0
python-decouple>=3.8
djangorestframework==3.16.1
django-cors-headers==4.9.0
boto3>=1.26.0
